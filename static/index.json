{
  "categories": [
    {
      "name": "Agents",
      "endpoints": [
        {
          "path": "/create_agent",
          "method": "POST",
          "summary": "Create agent",
          "description": "Create a new agent. Authorization typically required in headers.",
          "request": {
            "agent_name": "string",
            "agent_slug": "string",
            "permissions": {},
            "limits": {},
            "system-prompt": "string"
          },
          "response": {
            "agent_id": "string"
          },
          "when_to_use": "Call this when onboarding a new user, project, or tenant that needs its own isolated AI assistant with separate knowledge and memory.",
          "use_cases": [
            "Onboarding a new customer with a dedicated support agent",
            "Creating a per-user personalised assistant in a SaaS app",
            "Spinning up a project-specific agent that only knows about that project's documents",
            "Multi-tenant platforms: one agent per organisation or workspace"
          ],
          "notes": "agent_slug must be unique across your account and URL-safe (lowercase letters and hyphens only). Store the returned agent_id — it is required by every other endpoint.",
          "errors": [
            {"code": 400, "message": "Bad request — missing required fields (agent_name or agent_slug)"},
            {"code": 401, "message": "Unauthorized — missing or invalid API key"},
            {"code": 409, "message": "Conflict — agent_slug already exists in your account"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/create_agent' -H 'Content-Type: application/json' -H 'Authorization: Bearer <API_KEY>' -d '{\"agent_name\":\"test-agent\",\"agent_slug\":\"test-agent\"}'",
            "python": "import requests\n\nurl = \"__BASE__/create_agent\"\npayload = { 'agent_name': 'test-agent', 'agent_slug': 'test-agent', 'permissions': { 'run': True }, 'limits': { 'rpm': 10 }, 'description': 'Created by script' }\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=15)\n\nprint(response.text)",
            "javascript": "async function createAgent(baseUrl = null, apiKey = null, payload = null) {\n  baseUrl = baseUrl || (typeof process !== 'undefined' && process.env && process.env.BASE_URL);\n  if (!baseUrl) throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = apiKey;\n  const body = JSON.stringify(payload || { agent_name: 'test-agent', agent_slug: 'test-agent', permissions: { run: true }, limits: { rpm: 10 }, description: 'Created by script' });\n  const res = await fetch(`${baseUrl}/create_agent`, { method: 'POST', headers, body });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\ncreateAgent().then(console.log).catch(console.error);"
          }
        },
        {
          "path": "/list_agents",
          "method": "GET",
          "summary": "List agents (API key)",
          "description": "List agents available to the provided API key (Authorization header).",
          "request": null,
          "response": {
            "agents": []
          },
          "when_to_use": "Use at app startup to discover existing agents, populate an agent-picker UI, or verify an agent exists before making further calls.",
          "use_cases": [
            "Populating an agent dropdown/switcher in your UI",
            "Auditing all active agents in your account",
            "Verifying that a specific agent_slug has already been created",
            "Syncing your local state with the server after a session restart"
          ],
          "notes": "Returns only agents associated with the API key used. Both enabled and disabled agents are included — filter on the 'enabled' field client-side if you want active-only results.",
          "errors": [
            {"code": 401, "message": "Unauthorized — missing or invalid API key"}
          ],
          "code": {
            "curl": "curl -X GET '__BASE__/list_agents' -H 'Authorization: <API_KEY>'",
            "python": "import requests\n\nurl = \"__BASE__/list_agents\"\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.get(url, headers=headers, timeout=15)\n\nprint(response.text)",
            "javascript": "async function listAgents(baseUrl = null, apiKey = null) {\n  baseUrl = baseUrl || (typeof process !== 'undefined' && process.env && process.env.BASE_URL);\n  if (!baseUrl) throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = {} ;\n  if (apiKey) headers['Authorization'] = apiKey;\n  const res = await fetch(`${baseUrl}/list_agents`, { headers });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\nlistAgents().then(console.log).catch(console.error);"
          }
        },
        {
          "path": "/get_agent",
          "method": "GET",
          "summary": "Get agent by id",
          "description": "Retrieve a single agent by ID. Example shows a GET with JSON payload as used in tests (some servers accept JSON body on GET).",
          "request": {
            "agent_id": "string"
          },
          "response": {
            "agent": {}
          },
          "when_to_use": "Call this to retrieve full details of a specific agent, including its configuration, permissions, limits, and current status.",
          "use_cases": [
            "Displaying agent settings in an admin dashboard",
            "Verifying agent configuration before making updates",
            "Checking whether an agent is enabled or disabled before sending it traffic"
          ],
          "notes": "Pass agent_id as a query parameter. The response includes all agent fields including description, permissions, limits, and timestamps. Returns 404 if the agent does not exist.",
          "errors": [
            {"code": 400, "message": "Bad request — missing agent_id parameter"},
            {"code": 401, "message": "Unauthorized — missing or invalid API key"},
            {"code": 404, "message": "Not found — agent_id does not exist"}
          ],
          "code": {
            "curl": "curl -X GET '__BASE__/get_agent' -H 'Authorization: <API_KEY>' -G --data-urlencode 'agent_id=<AGENT_ID>'",
            "python": "import requests\n\nurl = \"__BASE__/get_agent\"\nparams = { 'agent_id': '<AGENT_ID>' }\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.get(url, params=params, headers=headers, timeout=15)\n\nprint(response.text)",
            "javascript": "async function getAgent(baseUrl = null, apiKey = null, agentId = null) {\n  baseUrl = baseUrl || (typeof process !== 'undefined' && process.env && process.env.BASE_URL);\n  if (!baseUrl) throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const url = new URL(`${baseUrl}/get_agent`);\n  if (agentId) url.searchParams.set('agent_id', agentId);\n  const headers = {} ;\n  if (apiKey) headers['Authorization'] = apiKey;\n  const res = await fetch(url.href, { headers });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\ngetAgent().then(console.log).catch(console.error);"
          }
        },
        {
          "path": "/update_agent",
          "method": "POST",
          "summary": "Update agent details",
          "description": "Update updatable fields for an agent. Authorization required.",
          "request": {
            "agent_id": "string",
            "updates": {}
          },
          "response": {
            "ok": true
          },
          "when_to_use": "Call this to modify an existing agent's configuration such as its description, permissions, or rate limits without recreating it.",
          "use_cases": [
            "Updating an agent's description after a product change",
            "Adjusting rate limits for an agent that is experiencing higher traffic",
            "Changing permissions to grant or revoke capabilities",
            "Renaming an agent for organizational purposes"
          ],
          "notes": "Only include fields you want to change in the updates object — omitted fields remain unchanged. agent_slug and agent_id cannot be modified after creation.",
          "errors": [
            {"code": 400, "message": "Bad request — missing agent_id or empty updates object"},
            {"code": 401, "message": "Unauthorized — missing or invalid API key"},
            {"code": 404, "message": "Not found — agent_id does not exist"},
            {"code": 422, "message": "Unprocessable entity — attempted to update an immutable field"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/update_agent' -H 'Content-Type: application/json' -H 'Authorization: <API_KEY>' -d '{\"agent_id\":\"<ID>\", \"updates\": {\"system_prompt\": \"updated\"}}'",
            "python": "import requests\n\nurl = \"__BASE__/update_agent\"\npayload = { 'agent_id': '<ID>', 'updates': { 'system_prompt': 'updated' } }\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=15)\n\nprint(response.text)",
            "javascript": "async function updateAgent(baseUrl = '__BASE__', apiKey = null, agentId = null, updates = {}) {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = apiKey;\n  const res = await fetch(`${baseUrl}/update_agent`, { method: 'POST', headers, body: JSON.stringify({ agent_id: agentId, updates }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\nupdateAgent().then(console.log).catch(console.error);"
          }
        },
        {
          "path": "/disable_agent",
          "method": "POST",
          "summary": "Disable agent",
          "description": "Disable an agent by id using Authorization header.",
          "request": {
            "agent_id": "string"
          },
          "response": {
            "ok": true
          },
          "when_to_use": "Call this to temporarily suspend an agent without deleting it. Disabled agents reject all chat and memory requests but retain their data.",
          "use_cases": [
            "Suspending a misbehaving agent while investigating an issue",
            "Pausing an agent during scheduled maintenance",
            "Temporarily blocking an agent when a customer's subscription lapses"
          ],
          "notes": "Disabling is non-destructive — all documents and memories are preserved. Re-enable with /enable_agent at any time. Disabled agents will return 404 to agent_chat callers.",
          "errors": [
            {"code": 400, "message": "Bad request — missing agent_id"},
            {"code": 401, "message": "Unauthorized — missing or invalid API key"},
            {"code": 404, "message": "Not found — agent_id does not exist"},
            {"code": 409, "message": "Conflict — agent is already disabled"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/disable_agent' -H 'Content-Type: application/json' -H 'Authorization: <API_KEY>' -d '{\"agent_id\":\"<ID>\"}'",
            "python": "import requests\n\nurl = \"__BASE__/disable_agent\"\npayload = { 'agent_id': '<ID>' }\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=15)\n\nprint(response.text)",
            "javascript": "async function disableAgent(baseUrl = '__BASE__', apiKey = null, agentId = null) {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = apiKey;\n  const res = await fetch(`${baseUrl}/disable_agent`, { method: 'POST', headers, body: JSON.stringify({ agent_id: agentId }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\ndisableAgent().then(console.log).catch(console.error);"
          }
        },
        {
          "path": "/enable_agent",
          "method": "POST",
          "summary": "Enable agent",
          "description": "Enable an agent by id using Authorization header.",
          "request": {
            "agent_id": "string"
          },
          "response": {
            "ok": true
          },
          "when_to_use": "Call this to re-activate a previously disabled agent so it can accept chat and memory requests again.",
          "use_cases": [
            "Re-enabling an agent after resolving an operational issue",
            "Restoring service after a customer renews their subscription",
            "Bringing an agent back online after maintenance"
          ],
          "notes": "Only works on agents that are currently disabled. If the agent is already enabled this is a no-op (returns ok). The agent resumes with all its previous documents and memories intact.",
          "errors": [
            {"code": 400, "message": "Bad request — missing agent_id"},
            {"code": 401, "message": "Unauthorized — missing or invalid API key"},
            {"code": 404, "message": "Not found — agent_id does not exist"},
            {"code": 409, "message": "Conflict — agent is already enabled"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/enable_agent' -H 'Content-Type: application/json' -H 'Authorization: <API_KEY>' -d '{\"agent_id\":\"<ID>\"}'",
            "python": "import requests\n\nurl = \"__BASE__/enable_agent\"\npayload = { 'agent_id': '<ID>' }\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=15)\n\nprint(response.text)",
            "javascript": "async function enableAgent(baseUrl = '__BASE__', apiKey = null, agentId = null) {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = apiKey;\n  const res = await fetch(`${baseUrl}/enable_agent`, { method: 'POST', headers, body: JSON.stringify({ agent_id: agentId }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\nenableAgent().then(console.log).catch(console.error);"
          }
        },
        {
          "path": "/delete_agent",
          "method": "POST",
          "summary": "Delete agent (permanent)",
          "description": "Permanently delete an agent by id for the authenticated user. Will also attempt to remove associated vector DB collections.",
          "request": {
            "agent_id": "string"
          },
          "response": {
            "ok": true,
            "message": "agent_deleted"
          },
          "when_to_use": "Use only when the agent and all its data (documents, memory, conversations) should be permanently removed. Prefer disable_agent for temporary suspension.",
          "use_cases": [
            "GDPR / right-to-erasure request — remove a user's entire AI profile",
            "Cleaning up test agents in a development environment",
            "Off-boarding a customer and purging their data"
          ],
          "notes": "This action is irreversible. It also attempts to delete the agent's vector DB collections. If vector deletion fails, the agent record is still removed from the database. Consider disable_agent if you may need to restore the agent later.",
          "errors": [
            {"code": 400, "message": "Bad request — missing agent_id"},
            {"code": 401, "message": "Unauthorized — invalid API key"},
            {"code": 404, "message": "Not found — agent does not exist or was already deleted"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/delete_agent' -H 'Content-Type: application/json' -H 'Authorization: <API_KEY>' -d '{\"agent_id\":\"<ID>\"}'",
            "python": "import requests\n\nurl = \"__BASE__/delete_agent\"\npayload = { 'agent_id': '<ID>' }\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=15)\n\nprint(response.text)",
            "javascript": "async function deleteAgent(baseUrl = '__BASE__', apiKey = null, agentId = null) {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = apiKey;\n  const res = await fetch(`${baseUrl}/delete_agent`, { method: 'POST', headers, body: JSON.stringify({ agent_id: agentId }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\ndeleteAgent().then(console.log).catch(console.error);"
          }
        },
        {
          "path": "/agent_chat",
          "method": "POST",
          "summary": "Chat with agent",
          "description": "Send a message to a specific agent and receive a reply. For high-speed, low-latency responses (<1.5s), set 'strategy' to 'fast'. Set to 'thinking' for deep planning/reflection. Default is 'auto' (backend routes dynamically).",
          "request": {
            "agent_id": "string",
            "message": "string",
            "strategy": "string (optional: 'auto', 'fast', 'thinking'; default 'auto'). Use 'fast' to guarantee low-latency snappier responses.",
            "options": {}
          },
          "response": {
            "reply": "string",
            "conversation_id": "string"
          },
          "when_to_use": "Call this whenever the end-user sends a message. It is the primary inference endpoint — it combines the agent's documents, memory, and the current conversation to produce a contextual reply.",
          "use_cases": [
            "Customer support chat widget backed by a product knowledge base",
            "Personal AI assistant that recalls previous conversations",
            "Domain-specific Q&A bot powered by uploaded documents",
            "Automated email-reply drafting with full conversation context"
          ],
          "notes": "This endpoint triggers LLM inference and may take 2–30 seconds. Use a client timeout of at least 120 seconds. Store the returned conversation_id and pass it on subsequent turns to maintain context continuity. Passing strategy 'auto' enables smart query-complexity based dynamic routing.",
          "errors": [
            {"code": 400, "message": "Bad request — missing agent_id or message"},
            {"code": 401, "message": "Unauthorized — invalid API key"},
            {"code": 404, "message": "Not found — agent_id does not exist or is disabled"},
            {"code": 503, "message": "Service unavailable — LLM provider is temporarily unreachable"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/agent_chat' -H 'Content-Type: application/json' -H 'Authorization: <API_KEY>' -d '{\"agent_id\":\"<AGENT_ID>\",\"message\":\"Hello\",\"strategy\":\"auto\"}'",
            "python": "import requests\n\nurl = \"__BASE__/agent_chat\"\npayload = { 'agent_id': '<AGENT_ID>', 'message': 'Hello', 'strategy': 'auto' }\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=120)\n\nprint(response.text)",
            "javascript": "async function chatWithAgent(baseUrl = '__BASE__', apiKey = null, agentId = null, message = 'Hello', strategy = 'auto') {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = apiKey;\n  const res = await fetch(`${baseUrl}/agent_chat`, { method: 'POST', headers, body: JSON.stringify({ agent_id: agentId, message, strategy }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\nchatWithAgent().then(console.log).catch(console.error);"
          }
        }
      ]
    },
    {
      "name": "Documents",
      "endpoints": [
        {
          "path": "/add_document",
          "method": "POST",
          "summary": "Add document(s) to agent",
          "description": "Add one or more documents to an agent's vector DB. Expects 'documents' array and matching 'ids' array of equal length.",
          "request": {
            "agent_id": "string",
            "documents": [
              "string"
            ],
            "ids": [
              "string"
            ],
            "metadata": {}
          },
          "response": {
            "ok": true,
            "message": "documents_added"
          },
          "when_to_use": "Call this to seed an agent's knowledge base. Documents are chunked, embedded, and stored in vector storage for semantic retrieval. Do this before agent_chat to give the agent access to your content.",
          "use_cases": [
            "Uploading a product knowledge base to back a support bot",
            "Feeding legal documents to a contract-analysis agent",
            "Adding FAQs so the agent can answer common questions precisely",
            "Syncing content updates when your source documents change"
          ],
          "notes": "The documents array and ids array must be the same length. IDs must be unique within the agent — use deterministic IDs (e.g. a filename hash) so you can update documents by ID later. Large documents are automatically chunked.",
          "errors": [
            {"code": 400, "message": "Bad request — documents and ids arrays have different lengths, or agent_id is missing"},
            {"code": 401, "message": "Unauthorized — invalid API key"},
            {"code": 413, "message": "Payload too large — reduce document size or split into smaller batches"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/add_document' -H 'Content-Type: application/json' -H 'Authorization: <API_KEY>' -d '{\"agent_id\":\"<ID>\",\"documents\":[\"doc 1 body\"],\"ids\":[\"doc-1\"],\"metadata\":{}}'",
            "python": "import requests\n\nurl = \"__BASE__/add_document\"\npayload = { 'agent_id': '<ID>', 'documents': ['doc 1 body'], 'ids': ['doc-1'], 'metadata': {} }\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=20)\n\nprint(response.text)",
            "javascript": "async function addDocument(baseUrl = '__BASE__', apiKey = null, agentId = null, docs = ['doc 1 body'], ids = ['doc-1'], metadata = {}) {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = apiKey;\n  const res = await fetch(`${baseUrl}/add_document`, { method: 'POST', headers, body: JSON.stringify({ agent_id: agentId, documents: docs, ids, metadata }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\naddDocument().then(console.log).catch(console.error);"
          }
        },
        {
          "path": "/update_document",
          "method": "POST",
          "summary": "Update existing document(s)",
          "description": "Update documents in an agent's vector DB by document id(s). Provide 'document_ids' and 'new_docs' fields.",
          "request": {
            "agent_id": "string",
            "document_ids": [
              "string"
            ],
            "new_docs": [
              "string"
            ],
            "metadata": {}
          },
          "response": {
            "ok": true,
            "message": "document_updated"
          },
          "when_to_use": "Call this to replace the content of existing documents in an agent's vector store while keeping the same document IDs.",
          "use_cases": [
            "Updating a FAQ document after policy changes",
            "Refreshing product documentation when a new version ships",
            "Correcting inaccurate content that the agent has been using in responses",
            "Syncing documents periodically from an external CMS"
          ],
          "notes": "document_ids and new_docs arrays must have the same length. The old embeddings are replaced — this is an upsert by ID. If a document_id does not exist, some implementations will insert it as new.",
          "errors": [
            {"code": 400, "message": "Bad request — document_ids and new_docs arrays differ in length, or agent_id missing"},
            {"code": 401, "message": "Unauthorized — missing or invalid API key"},
            {"code": 404, "message": "Not found — agent or document collection does not exist"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/update_document' -H 'Content-Type: application/json' -H 'Authorization: <API_KEY>' -d '{\"agent_id\":\"<ID>\",\"document_ids\":[\"doc-1\"],\"new_docs\":[\"updated body\"],\"metadata\":{}}'",
            "python": "import requests\n\nurl = \"__BASE__/update_document\"\npayload = { 'agent_id': '<ID>', 'document_ids': ['doc-1'], 'new_docs': ['updated body'], 'metadata': {} }\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=20)\n\nprint(response.text)",
            "javascript": "async function updateDocument(baseUrl = '__BASE__', apiKey = null, agentId = null, ids = ['doc-1'], docs = ['updated body'], metadata = {}) {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = apiKey;\n  const res = await fetch(`${baseUrl}/update_document`, { method: 'POST', headers, body: JSON.stringify({ agent_id: agentId, document_ids: ids, new_docs: docs, metadata }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\nupdateDocument().then(console.log).catch(console.error);"
          }
        },
        {
          "path": "/update_document_metadata",
          "method": "POST",
          "summary": "Update document metadata",
          "description": "Update only the metadata for a given document id in an agent's vector DB.",
          "request": {
            "agent_id": "string",
            "document_id": "string",
            "metadata": {}
          },
          "response": {
            "ok": true,
            "message": "document_metadata_updated"
          },
          "when_to_use": "Call this to change metadata (tags, source, category) on a document without re-embedding its content.",
          "use_cases": [
            "Tagging documents with a new category for filtered retrieval",
            "Updating the source URL after a content migration",
            "Adding version or last-modified timestamps to track freshness"
          ],
          "notes": "Only metadata is changed — the document text and its vector embedding remain untouched. This is cheaper and faster than a full document update when only labels need to change.",
          "errors": [
            {"code": 400, "message": "Bad request — missing agent_id or document_id"},
            {"code": 401, "message": "Unauthorized — missing or invalid API key"},
            {"code": 404, "message": "Not found — document_id does not exist in this agent's collection"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/update_document_metadata' -H 'Content-Type: application/json' -H 'Authorization: <API_KEY>' -d '{\"agent_id\":\"<ID>\",\"document_id\":\"doc-1\",\"metadata\":{\"source\":\"upload\"}}'",
            "python": "import requests\n\nurl = \"__BASE__/update_document_metadata\"\npayload = { 'agent_id': '<ID>', 'document_id': 'doc-1', 'metadata': { 'source': 'upload' } }\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=20)\n\nprint(response.text)",
            "javascript": "async function updateDocumentMetadata(baseUrl = '__BASE__', apiKey = null, agentId = null, documentId = 'doc-1', metadata = { source: 'upload' }) {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = apiKey;\n  const res = await fetch(`${baseUrl}/update_document_metadata`, { method: 'POST', headers, body: JSON.stringify({ agent_id: agentId, document_id: documentId, metadata }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\nupdateDocumentMetadata().then(console.log).catch(console.error);"
          }
        },
        {
          "path": "/search_documents",
          "method": "POST",
          "summary": "Search agent documents",
          "description": "Perform a vector/semantic search against an agent's document collection. Provide 'query' and optional 'top_k'.",
          "request": {
            "agent_id": "string",
            "query": "string",
            "top_k": 5
          },
          "response": {
            "results": []
          },
          "when_to_use": "Use when you need to retrieve specific document chunks by semantic similarity — useful for building a custom RAG pipeline or debugging what context the agent can see for a given query.",
          "use_cases": [
            "Building a custom RAG pipeline outside of agent_chat",
            "Debugging why the agent answered incorrectly by inspecting which chunks were retrieved",
            "Adding a 'search your knowledge base' feature to your UI",
            "Pre-fetching context before constructing a custom LLM prompt"
          ],
          "notes": "top_k defaults to 5. Increasing it improves recall but may add noise. Results are ordered by cosine similarity score (highest first). This endpoint does NOT run LLM inference — it only returns matching document chunks.",
          "errors": [
            {"code": 400, "message": "Bad request — missing query or agent_id"},
            {"code": 401, "message": "Unauthorized — invalid API key"},
            {"code": 404, "message": "Not found — agent has no documents or the collection does not exist yet"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/search_documents' -H 'Content-Type: application/json' -H 'Authorization: <API_KEY>' -d '{\"agent_id\":\"<ID>\",\"query\":\"what is X\",\"top_k\":5 }'",
            "python": "import requests\n\nurl = \"__BASE__/search_documents\"\npayload = { 'agent_id': '<ID>', 'query': 'what is X', 'top_k': 5 }\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=20)\n\nprint(response.text)",
            "javascript": "async function searchDocuments(baseUrl = '__BASE__', apiKey = null, agentId = null, query = 'what is X', topK = 5) {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = apiKey;\n  const res = await fetch(`${baseUrl}/search_documents`, { method: 'POST', headers, body: JSON.stringify({ agent_id: agentId, query, top_k: topK }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\nsearchDocuments().then(console.log).catch(console.error);"
          }
        }
      ]
    },
    {
      "name": "Memory",
      "endpoints": [
        {
          "path": "/create_memory",
          "method": "POST",
          "summary": "Initialize memory system",
          "description": "Create/initialize a SimpleMem memory system for an agent. This creates a ChromaDB collection for storing memory entries. Set 'clear_db' to true to clear existing memories.",
          "request": {
            "agent_id": "string",
            "clear_db": false
          },
          "response": {
            "ok": true,
            "message": "memory_system_initialized",
            "agent_id": "string",
            "cleared": false
          },
          "when_to_use": "Call once before using any other /memory endpoints for a given agent. This initialises the ChromaDB collection that stores structured memory entries. If the collection already exists it is a no-op unless clear_db is true.",
          "use_cases": [
            "First-time setup: call immediately after creating a new agent",
            "Resetting an agent's memory during development or testing with clear_db: true",
            "Re-initialising after a database migration or storage reset"
          ],
          "notes": "This must be called before add_memory, read_memory, or get_context — those endpoints will fail if the memory system has not been initialised first. Setting clear_db: true permanently erases all stored memories for that agent.",
          "errors": [
            {"code": 400, "message": "Bad request — missing agent_id"},
            {"code": 401, "message": "Unauthorized — invalid API key"},
            {"code": 500, "message": "Internal server error — vector DB initialisation failed (check server logs)"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/create_memory' -H 'Content-Type: application/json' -H 'Authorization: Bearer <API_KEY>' -d '{\"agent_id\":\"<AGENT_ID>\",\"clear_db\":false}'",
            "python": "import requests\n\nurl = \"__BASE__/create_memory\"\npayload = { 'agent_id': '<AGENT_ID>', 'clear_db': False }\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=30)\n\nprint(response.json())",
            "javascript": "async function createMemory(baseUrl = '__BASE__', apiKey = null, agentId = null, clearDb = false) {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = `Bearer ${apiKey}`;\n  const res = await fetch(`${baseUrl}/create_memory`, { method: 'POST', headers, body: JSON.stringify({ agent_id: agentId, clear_db: clearDb }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\ncreateMemory().then(console.log).catch(console.error);"
          }
        },
        {
          "path": "/process_raw",
          "method": "POST",
          "summary": "Process dialogues via LLM",
          "description": "Process raw dialogues through LLM to extract structured memory entries. Flow: ADD_DIALOGUE → LLM → JSON RESPONSE → N Memory units → Vector Store. Each dialogue is processed to extract facts, entities, timestamps, and keywords.",
          "request": {
            "agent_id": "string",
            "dialogues": [
              {
                "speaker": "string",
                "content": "string",
                "timestamp": "ISO8601 string (optional)"
              }
            ]
          },
          "response": {
            "ok": true,
            "message": "dialogues_processed",
            "agent_id": "string",
            "dialogues_processed": 0
          },
          "when_to_use": "Call this when you have raw conversation transcripts or dialogue logs and want the LLM to automatically extract structured memory entries (facts, entities, timestamps) from them.",
          "use_cases": [
            "Ingesting chat logs from a customer support platform into agent memory",
            "Processing meeting transcripts to extract action items and decisions",
            "Converting unstructured interview notes into searchable memory entries",
            "Archiving conversation history from another system into structured memory"
          ],
          "notes": "This endpoint triggers LLM inference and may take 10-120 seconds depending on dialogue volume. Each dialogue is processed independently. Use add_memory instead if you already have structured data to avoid unnecessary LLM costs.",
          "errors": [
            {"code": 400, "message": "Bad request — missing agent_id or empty dialogues array"},
            {"code": 401, "message": "Unauthorized — missing or invalid API key"},
            {"code": 404, "message": "Not found — memory system not initialised (call create_memory first)"},
            {"code": 503, "message": "Service unavailable — LLM processing failed"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/process_raw' -H 'Content-Type: application/json' -H 'Authorization: Bearer <API_KEY>' -d '{\"agent_id\":\"<AGENT_ID>\",\"dialogues\":[{\"speaker\":\"Alice\",\"content\":\"Meeting at Starbucks tomorrow at 2pm\",\"timestamp\":\"2025-01-22T10:00:00\"}]}'",
            "python": "import requests\n\nurl = \"__BASE__/process_raw\"\npayload = {\n    'agent_id': '<AGENT_ID>',\n    'dialogues': [\n        {'speaker': 'Alice', 'content': 'Meeting at Starbucks tomorrow at 2pm', 'timestamp': '2025-01-22T10:00:00'},\n        {'speaker': 'Bob', 'content': 'I will bring the documents', 'timestamp': '2025-01-22T10:01:00'}\n    ]\n}\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=120)\n\nprint(response.json())",
            "javascript": "async function processRaw(baseUrl = '__BASE__', apiKey = null, agentId = null, dialogues = []) {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = `Bearer ${apiKey}`;\n  const res = await fetch(`${baseUrl}/process_raw`, { method: 'POST', headers, body: JSON.stringify({ agent_id: agentId, dialogues }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\nconst dialogues = [{ speaker: 'Alice', content: 'Meeting at 2pm', timestamp: new Date().toISOString() }];\nprocessRaw(null, null, '<AGENT_ID>', dialogues).then(console.log).catch(console.error);"
          }
        },
        {
          "path": "/add_memory",
          "method": "POST",
          "summary": "Direct memory save (no LLM)",
          "description": "Directly save pre-structured memory entries without LLM processing. Use this when you already have structured memory data (lossless_restatement, keywords, etc.) and want to bypass the LLM extraction step.",
          "request": {
            "agent_id": "string",
            "memories": [
              {
                "lossless_restatement": "string",
                "keywords": [
                  "string"
                ],
                "timestamp": "ISO8601 (optional)",
                "location": "string (optional)",
                "persons": [
                  "string"
                ],
                "entities": [
                  "string"
                ],
                "topic": "string (optional)"
              }
            ]
          },
          "response": {
            "ok": true,
            "message": "memories_added",
            "agent_id": "string",
            "entries_added": 0,
            "entry_ids": []
          },
          "when_to_use": "Use to store structured facts directly in the agent's memory, bypassing LLM extraction. Prefer process_raw when you have raw conversation transcripts to process.",
          "use_cases": [
            "Storing a user's explicitly stated preference (e.g. 'I prefer Python over JavaScript')",
            "Saving structured facts from a form submission or structured event",
            "Persisting key decisions made in a conversation for future sessions",
            "Building up a user profile incrementally from explicit data"
          ],
          "notes": "Unlike process_raw, this bypasses LLM processing — the memory is stored exactly as you provide it. Supply meaningful keywords to improve future recall accuracy. The lossless_restatement field should be a complete, self-contained statement.",
          "errors": [
            {"code": 400, "message": "Bad request — missing agent_id or memories array, or memories missing lossless_restatement"},
            {"code": 401, "message": "Unauthorized — invalid API key"},
            {"code": 404, "message": "Not found — memory system not initialised (call create_memory first)"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/add_memory' -H 'Content-Type: application/json' -H 'Authorization: Bearer <API_KEY>' -d '{\"agent_id\":\"<AGENT_ID>\",\"memories\":[{\"lossless_restatement\":\"Alice scheduled a meeting with Bob at Starbucks on January 22, 2025 at 2pm\",\"keywords\":[\"meeting\",\"Starbucks\",\"Alice\",\"Bob\"],\"persons\":[\"Alice\",\"Bob\"],\"location\":\"Starbucks\",\"timestamp\":\"2025-01-22T14:00:00\",\"topic\":\"meeting scheduling\"}]}'",
            "python": "import requests\n\nurl = \"__BASE__/add_memory\"\npayload = {\n    'agent_id': '<AGENT_ID>',\n    'memories': [\n        {\n            'lossless_restatement': 'Alice scheduled a meeting with Bob at Starbucks on January 22, 2025 at 2pm',\n            'keywords': ['meeting', 'Starbucks', 'Alice', 'Bob'],\n            'persons': ['Alice', 'Bob'],\n            'location': 'Starbucks',\n            'timestamp': '2025-01-22T14:00:00',\n            'topic': 'meeting scheduling'\n        }\n    ]\n}\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=30)\n\nprint(response.json())",
            "javascript": "async function addMemory(baseUrl = '__BASE__', apiKey = null, agentId = null, memories = []) {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = `Bearer ${apiKey}`;\n  const res = await fetch(`${baseUrl}/add_memory`, { method: 'POST', headers, body: JSON.stringify({ agent_id: agentId, memories }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\nconst memories = [{ lossless_restatement: 'Example fact', keywords: ['example'], persons: [], entities: [] }];\naddMemory(null, null, '<AGENT_ID>', memories).then(console.log).catch(console.error);"
          }
        },
        {
          "path": "/read_memory",
          "method": "POST",
          "summary": "Hybrid search retrieval",
          "description": "Search memories using hybrid retrieval combining semantic (vector similarity), keyword (BM25-style), and structured (metadata filtering) search. Optionally enable reflection for multi-round retrieval.",
          "request": {
            "agent_id": "string",
            "query": "string",
            "top_k": 5,
            "enable_reflection": false
          },
          "response": {
            "ok": true,
            "agent_id": "string",
            "query": "string",
            "results_count": 0,
            "results": [
              {
                "entry_id": "string",
                "lossless_restatement": "string",
                "keywords": [],
                "timestamp": "string",
                "location": "string",
                "persons": [],
                "entities": [],
                "topic": "string"
              }
            ]
          },
          "when_to_use": "Call this to search an agent's memory using hybrid retrieval (semantic + keyword + metadata). Use it when you need raw memory results without an LLM-generated answer.",
          "use_cases": [
            "Building a custom RAG pipeline where you supply your own answer generation",
            "Debugging memory retrieval quality by inspecting raw results",
            "Showing users a list of related memories in a UI",
            "Pre-filtering memories before passing them to a specialized prompt"
          ],
          "notes": "Combines vector similarity, keyword matching, and metadata filtering. Set enable_reflection to true for multi-round retrieval that refines results (slower but higher recall). top_k defaults to 5.",
          "errors": [
            {"code": 400, "message": "Bad request — missing agent_id or query"},
            {"code": 401, "message": "Unauthorized — missing or invalid API key"},
            {"code": 404, "message": "Not found — memory system not initialised (call create_memory first)"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/read_memory' -H 'Content-Type: application/json' -H 'Authorization: Bearer <API_KEY>' -d '{\"agent_id\":\"<AGENT_ID>\",\"query\":\"When is the meeting with Bob?\",\"top_k\":5,\"enable_reflection\":false}'",
            "python": "import requests\n\nurl = \"__BASE__/read_memory\"\npayload = {\n    'agent_id': '<AGENT_ID>',\n    'query': 'When is the meeting with Bob?',\n    'top_k': 5,\n    'enable_reflection': False\n}\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=60)\n\nprint(response.json())",
            "javascript": "async function readMemory(baseUrl = '__BASE__', apiKey = null, agentId = null, query = '', topK = 5, enableReflection = false) {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = `Bearer ${apiKey}`;\n  const res = await fetch(`${baseUrl}/read_memory`, { method: 'POST', headers, body: JSON.stringify({ agent_id: agentId, query, top_k: topK, enable_reflection: enableReflection }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\nreadMemory(null, null, '<AGENT_ID>', 'When is the meeting?').then(console.log).catch(console.error);"
          }
        },
        {
          "path": "/get_context",
          "method": "POST",
          "summary": "Q&A with memory context",
          "description": "Ask a question and get an LLM-generated answer using memory context. Full Q&A flow: Query → HybridRetrieval → AnswerGenerator → Response. Returns both the answer and the memory contexts used.",
          "request": {
            "agent_id": "string",
            "question": "string"
          },
          "response": {
            "ok": true,
            "agent_id": "string",
            "question": "string",
            "answer": "string",
            "contexts_used": [
              {
                "entry_id": "string",
                "lossless_restatement": "string",
                "topic": "string"
              }
            ]
          },
          "when_to_use": "Call when you need an LLM-generated answer grounded in the agent's stored memories. It handles the full retrieval-augmented generation cycle internally — use it when you want answers, not raw chunks.",
          "use_cases": [
            "Answering 'what did we discuss last time?' from a user's perspective",
            "Showing users what the agent remembers about a specific topic",
            "Building a memory-backed Q&A interface without a custom RAG pipeline",
            "Summarising past interactions or decisions on a given subject"
          ],
          "notes": "agent_chat already calls this internally — only use get_context if you are building a custom pipeline and want the answer + source contexts separately. The contexts_used array shows exactly which memories were retrieved, useful for debugging or citations.",
          "errors": [
            {"code": 400, "message": "Bad request — missing question or agent_id"},
            {"code": 401, "message": "Unauthorized — invalid API key"},
            {"code": 404, "message": "Not found — memory system not initialised for this agent"},
            {"code": 503, "message": "Service unavailable — LLM answer generation failed"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/get_context' -H 'Content-Type: application/json' -H 'Authorization: Bearer <API_KEY>' -d '{\"agent_id\":\"<AGENT_ID>\",\"question\":\"What did Alice and Bob discuss about the meeting?\"}'",
            "python": "import requests\n\nurl = \"__BASE__/get_context\"\npayload = {\n    'agent_id': '<AGENT_ID>',\n    'question': 'What did Alice and Bob discuss about the meeting?'\n}\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=120)\n\nresult = response.json()\nprint('Answer:', result.get('answer'))\nprint('Contexts used:', result.get('contexts_used'))",
            "javascript": "async function getContext(baseUrl = '__BASE__', apiKey = null, agentId = null, question = '') {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = `Bearer ${apiKey}`;\n  const res = await fetch(`${baseUrl}/get_context`, { method: 'POST', headers, body: JSON.stringify({ agent_id: agentId, question }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\ngetContext(null, null, '<AGENT_ID>', 'What is the meeting about?').then(r => { console.log('Answer:', r.answer); console.log('Contexts:', r.contexts_used); }).catch(console.error);"
          }
        },
        {
          "path": "/update_memory",
          "method": "POST",
          "summary": "Update memory entry",
          "description": "Update an existing memory entry in ChromaDB. You can update the lossless_restatement (document content) and/or metadata fields (timestamp, location, persons, entities, topic, keywords).",
          "request": {
            "agent_id": "string",
            "entry_id": "string",
            "updates": {
              "lossless_restatement": "string (optional)",
              "timestamp": "string (optional)",
              "location": "string (optional)",
              "persons": [
                "string"
              ],
              "entities": [
                "string"
              ],
              "topic": "string (optional)",
              "keywords": [
                "string"
              ]
            }
          },
          "response": {
            "ok": true,
            "message": "memory_updated",
            "agent_id": "string",
            "entry_id": "string"
          },
          "when_to_use": "Call this to correct or enrich an existing memory entry — for example, updating a fact that has changed or adding missing metadata like location or persons.",
          "use_cases": [
            "Correcting a memory entry that contains outdated information",
            "Adding a person's name to a memory after learning who was involved",
            "Updating a timestamp when a scheduled event is rescheduled",
            "Enriching a memory with additional keywords to improve future retrieval"
          ],
          "notes": "Only include fields you want to change in the updates object. If you update lossless_restatement, the vector embedding is also re-computed. Metadata-only updates are faster since they skip re-embedding.",
          "errors": [
            {"code": 400, "message": "Bad request — missing agent_id or entry_id"},
            {"code": 401, "message": "Unauthorized — missing or invalid API key"},
            {"code": 404, "message": "Not found — entry_id does not exist in this agent's memory"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/update_memory' -H 'Content-Type: application/json' -H 'Authorization: Bearer <API_KEY>' -d '{\"agent_id\":\"<AGENT_ID>\",\"entry_id\":\"<ENTRY_ID>\",\"updates\":{\"lossless_restatement\":\"Updated: Alice rescheduled the meeting to 3pm\",\"timestamp\":\"2025-01-22T15:00:00\"}}'",
            "python": "import requests\n\nurl = \"__BASE__/update_memory\"\npayload = {\n    'agent_id': '<AGENT_ID>',\n    'entry_id': '<ENTRY_ID>',\n    'updates': {\n        'lossless_restatement': 'Updated: Alice rescheduled the meeting to 3pm',\n        'timestamp': '2025-01-22T15:00:00',\n        'topic': 'meeting rescheduling'\n    }\n}\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=30)\n\nprint(response.json())",
            "javascript": "async function updateMemory(baseUrl = '__BASE__', apiKey = null, agentId = null, entryId = null, updates = {}) {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = `Bearer ${apiKey}`;\n  const res = await fetch(`${baseUrl}/update_memory`, { method: 'POST', headers, body: JSON.stringify({ agent_id: agentId, entry_id: entryId, updates }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\nupdateMemory(null, null, '<AGENT_ID>', '<ENTRY_ID>', { topic: 'updated topic' }).then(console.log).catch(console.error);"
          }
        },
        {
          "path": "/delete_memory",
          "method": "POST",
          "summary": "Delete memory entries",
          "description": "Delete one or more memory entries from ChromaDB by their entry IDs. This permanently removes the memory entries from the agent's vector store.",
          "request": {
            "agent_id": "string",
            "entry_ids": [
              "string"
            ]
          },
          "response": {
            "ok": true,
            "message": "memories_deleted",
            "agent_id": "string",
            "deleted_count": 0,
            "entry_ids": []
          },
          "when_to_use": "Call this to permanently remove specific memory entries by their IDs. Use for GDPR erasure, correcting bad data, or cleaning up test entries.",
          "use_cases": [
            "Honoring a user's right-to-be-forgotten request by deleting specific memories",
            "Removing incorrect or hallucinated memory entries",
            "Cleaning up test data from development sessions",
            "Purging sensitive information that was accidentally stored"
          ],
          "notes": "This action is irreversible — deleted memories cannot be recovered. Pass one or more entry_ids. Use list_memories or read_memory first to identify which IDs to delete.",
          "errors": [
            {"code": 400, "message": "Bad request — missing agent_id or empty entry_ids array"},
            {"code": 401, "message": "Unauthorized — missing or invalid API key"},
            {"code": 404, "message": "Not found — memory system not initialised or entry_ids do not exist"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/delete_memory' -H 'Content-Type: application/json' -H 'Authorization: Bearer <API_KEY>' -d '{\"agent_id\":\"<AGENT_ID>\",\"entry_ids\":[\"<ENTRY_ID_1>\",\"<ENTRY_ID_2>\"]}'",
            "python": "import requests\n\nurl = \"__BASE__/delete_memory\"\npayload = {\n    'agent_id': '<AGENT_ID>',\n    'entry_ids': ['<ENTRY_ID_1>', '<ENTRY_ID_2>']\n}\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=30)\n\nprint(response.json())",
            "javascript": "async function deleteMemory(baseUrl = '__BASE__', apiKey = null, agentId = null, entryIds = []) {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = `Bearer ${apiKey}`;\n  const res = await fetch(`${baseUrl}/delete_memory`, { method: 'POST', headers, body: JSON.stringify({ agent_id: agentId, entry_ids: entryIds }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\ndeleteMemory(null, null, '<AGENT_ID>', ['<ENTRY_ID>']).then(console.log).catch(console.error);"
          }
        }
      ]
    },
    {
      "name": "Analytics & Stats",
      "endpoints": [
        {
          "path": "/agent_stats",
          "method": "POST",
          "summary": "Get agent statistics",
          "description": "Get comprehensive statistics for an agent including total memories, documents, topic breakdown, unique persons/locations mentioned, and activity metadata.",
          "request": {
            "agent_id": "string"
          },
          "response": {
            "ok": true,
            "agent_id": "string",
            "agent_name": "string",
            "agent_status": "string",
            "statistics": {
              "total_memories": 0,
              "total_documents": 0,
              "topics": {},
              "unique_persons": [],
              "unique_locations": [],
              "persons_count": 0,
              "locations_count": 0
            },
            "created_at": "string",
            "updated_at": "string"
          },
          "when_to_use": "Call this to get a comprehensive overview of an agent's data — how many memories and documents it has, topic distribution, and activity timestamps.",
          "use_cases": [
            "Displaying an agent health dashboard in an admin panel",
            "Monitoring memory growth over time for capacity planning",
            "Auditing which topics and persons an agent knows about",
            "Verifying that data ingestion pipelines are working correctly"
          ],
          "notes": "Statistics are computed on-demand and may be slightly delayed for very large collections. The topics breakdown shows unique topics and their frequency across all memories.",
          "errors": [
            {"code": 400, "message": "Bad request — missing agent_id"},
            {"code": 401, "message": "Unauthorized — missing or invalid API key"},
            {"code": 404, "message": "Not found — agent_id does not exist"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/agent_stats' -H 'Content-Type: application/json' -H 'Authorization: Bearer <API_KEY>' -d '{\"agent_id\":\"<AGENT_ID>\"}'",
            "python": "import requests\n\nurl = \"__BASE__/agent_stats\"\npayload = { 'agent_id': '<AGENT_ID>' }\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=30)\n\nprint(response.json())",
            "javascript": "async function getAgentStats(baseUrl = '__BASE__', apiKey = null, agentId = null) {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = `Bearer ${apiKey}`;\n  const res = await fetch(`${baseUrl}/agent_stats`, { method: 'POST', headers, body: JSON.stringify({ agent_id: agentId }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\ngetAgentStats(null, null, '<AGENT_ID>').then(console.log).catch(console.error);"
          }
        },
        {
          "path": "/api_usage",
          "method": "POST",
          "summary": "Get API usage statistics",
          "description": "Get API usage statistics for the authenticated user including total API calls, calls by endpoint, rate limit status, agents count, and billing period information.",
          "request": {},
          "response": {
            "ok": true,
            "user_id": "string",
            "billing_period": {
              "start": "string",
              "end": "string"
            },
            "agents": {
              "total": 0,
              "active": 0,
              "disabled": 0
            },
            "api_calls": {
              "total": 0,
              "by_endpoint": {},
              "limit": 10000,
              "remaining": 10000
            },
            "memory_storage": {
              "total_memories": 0,
              "storage_mb": 0,
              "limit_mb": 1000
            },
            "rate_limits": {
              "requests_per_minute": 60,
              "requests_per_day": 10000
            }
          },
          "when_to_use": "Call this to check your account's API consumption, rate limit status, and remaining quota within the current billing period.",
          "use_cases": [
            "Displaying a usage meter in your application's billing page",
            "Alerting when approaching rate limits or quota caps",
            "Generating monthly usage reports for internal accounting",
            "Debugging 429 errors by checking remaining request allowance"
          ],
          "notes": "Authenticated by API key — returns usage for the key's associated account. Counters reset at the start of each billing period. The rate_limits object shows your current plan's limits.",
          "errors": [
            {"code": 401, "message": "Unauthorized — missing or invalid API key"},
            {"code": 500, "message": "Internal server error — usage tracking service unavailable"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/api_usage' -H 'Content-Type: application/json' -H 'Authorization: Bearer <API_KEY>'",
            "python": "import requests\n\nurl = \"__BASE__/api_usage\"\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json={}, headers=headers, timeout=30)\n\nprint(response.json())",
            "javascript": "async function getApiUsage(baseUrl = '__BASE__', apiKey = null) {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = `Bearer ${apiKey}`;\n  const res = await fetch(`${baseUrl}/api_usage`, { method: 'POST', headers, body: '{}' });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\ngetApiUsage().then(console.log).catch(console.error);"
          }
        },
        {
          "path": "/health_detailed",
          "method": "GET",
          "summary": "Detailed health check",
          "description": "Get detailed health status of all backend services including database connectivity, vector store status, and LLM service status. Returns 503 if any critical service is unhealthy.",
          "request": null,
          "response": {
            "ok": true,
            "timestamp": "string",
            "version": "2.0.0",
            "services": {
              "database": {
                "status": "healthy",
                "type": "supabase"
              },
              "vector_store": {
                "status": "healthy",
                "type": "chromadb"
              },
              "llm": {
                "status": "healthy",
                "provider": "openai"
              }
            }
          },
          "when_to_use": "Call this to verify that all backend services (database, vector store, LLM) are operational before routing traffic or as part of automated monitoring.",
          "use_cases": [
            "Automated health checks from an uptime monitoring service",
            "Pre-flight validation before running a batch data import",
            "Diagnosing which subsystem is causing failures",
            "Load balancer health probes to route away from unhealthy instances"
          ],
          "notes": "Returns HTTP 200 if all services are healthy, 503 if any critical service is degraded. No authentication required — safe to expose to monitoring tools. Check individual service statuses in the response body.",
          "errors": [
            {"code": 503, "message": "Service unavailable — one or more backend services are unhealthy (check response body for details)"}
          ],
          "code": {
            "curl": "curl -X GET '__BASE__/health_detailed'",
            "python": "import requests\n\nurl = \"__BASE__/health_detailed\"\n\nresponse = requests.get(url, timeout=10)\n\nprint(response.json())",
            "javascript": "async function healthDetailed(baseUrl = '__BASE__') {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const res = await fetch(`${baseUrl}/health_detailed`);\n  return { status: res.status, body: await res.json() };\n}\nhealthDetailed().then(console.log).catch(console.error);"
          }
        }
      ]
    },
    {
      "name": "Bulk Operations",
      "endpoints": [
        {
          "path": "/list_memories",
          "method": "POST",
          "summary": "List all memories (paginated)",
          "description": "List all memories for an agent with pagination support. Supports filtering by topic or person. Returns metadata for each memory entry.",
          "request": {
            "agent_id": "string",
            "limit": 50,
            "offset": 0,
            "filter_topic": "string (optional)",
            "filter_person": "string (optional)"
          },
          "response": {
            "ok": true,
            "agent_id": "string",
            "total_count": 0,
            "limit": 50,
            "offset": 0,
            "has_more": false,
            "memories": []
          },
          "when_to_use": "Call this to paginate through all memory entries for an agent, optionally filtering by topic or person. Use for bulk inspection or export workflows.",
          "use_cases": [
            "Building a memory browser UI with pagination",
            "Auditing all memories stored for a specific person (GDPR subject access request)",
            "Finding memory entries on a specific topic for review or deletion",
            "Iterating through all memories for a custom export or transformation pipeline"
          ],
          "notes": "Use limit and offset for pagination. Filter by filter_topic or filter_person to narrow results. Returns metadata for each entry including entry_id, which you can use with update_memory or delete_memory.",
          "errors": [
            {"code": 400, "message": "Bad request — missing agent_id"},
            {"code": 401, "message": "Unauthorized — missing or invalid API key"},
            {"code": 404, "message": "Not found — memory system not initialised for this agent"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/list_memories' -H 'Content-Type: application/json' -H 'Authorization: Bearer <API_KEY>' -d '{\"agent_id\":\"<AGENT_ID>\",\"limit\":50,\"offset\":0}'",
            "python": "import requests\n\nurl = \"__BASE__/list_memories\"\npayload = {\n    'agent_id': '<AGENT_ID>',\n    'limit': 50,\n    'offset': 0,\n    'filter_topic': None  # Optional: filter by topic\n}\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=30)\n\nresult = response.json()\nprint(f\"Total: {result.get('total_count')}, Has More: {result.get('has_more')}\")\nfor mem in result.get('memories', []):\n    print(f\"  - {mem.get('lossless_restatement')[:50]}...\")",
            "javascript": "async function listMemories(baseUrl = '__BASE__', apiKey = null, agentId = null, limit = 50, offset = 0) {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = `Bearer ${apiKey}`;\n  const res = await fetch(`${baseUrl}/list_memories`, { method: 'POST', headers, body: JSON.stringify({ agent_id: agentId, limit, offset }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\nlistMemories(null, null, '<AGENT_ID>').then(r => {\n  console.log('Total:', r.total_count);\n  r.memories.forEach(m => console.log('-', m.lossless_restatement?.slice(0, 50)));\n}).catch(console.error);"
          }
        },
        {
          "path": "/bulk_add_memory",
          "method": "POST",
          "summary": "Bulk add memories",
          "description": "Add multiple memories in a single request (max 100). Optimized for high-volume memory ingestion. Returns individual success/error status for each memory.",
          "request": {
            "agent_id": "string",
            "memories": [
              {
                "lossless_restatement": "string",
                "keywords": [
                  "string"
                ],
                "timestamp": "string (optional)",
                "location": "string (optional)",
                "persons": [
                  "string"
                ],
                "entities": [
                  "string"
                ],
                "topic": "string (optional)"
              }
            ],
            "skip_duplicates": false
          },
          "response": {
            "ok": true,
            "agent_id": "string",
            "memories_added": 0,
            "memories_skipped": 0,
            "entry_ids": [],
            "errors": null
          },
          "when_to_use": "Call this when you need to ingest many pre-structured memories at once (up to 100 per request). More efficient than calling /add_memory repeatedly in a loop.",
          "use_cases": [
            "Initial data migration from another system into agent memory",
            "Batch-loading user preferences collected from a form or survey",
            "Restoring memories from a backup without using /import_memories",
            "Programmatically seeding an agent with a knowledge base of structured facts"
          ],
          "notes": "Maximum 100 memories per request. Set skip_duplicates to true to avoid reinserting memories that already exist (matched by lossless_restatement). Returns per-entry status so you can identify and retry failures.",
          "errors": [
            {"code": 400, "message": "Bad request — missing agent_id or memories array, or exceeds 100 entry limit"},
            {"code": 401, "message": "Unauthorized — missing or invalid API key"},
            {"code": 404, "message": "Not found — memory system not initialised (call create_memory first)"},
            {"code": 413, "message": "Payload too large — reduce batch size"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/bulk_add_memory' -H 'Content-Type: application/json' -H 'Authorization: Bearer <API_KEY>' -d '{\"agent_id\":\"<AGENT_ID>\",\"memories\":[{\"lossless_restatement\":\"Memory 1\",\"keywords\":[\"test\"]},{\"lossless_restatement\":\"Memory 2\",\"keywords\":[\"test\"]}]}'",
            "python": "import requests\n\nurl = \"__BASE__/bulk_add_memory\"\npayload = {\n    'agent_id': '<AGENT_ID>',\n    'memories': [\n        {'lossless_restatement': 'Alice prefers morning meetings', 'keywords': ['meetings', 'schedule'], 'persons': ['Alice']},\n        {'lossless_restatement': 'Bob works remotely on Fridays', 'keywords': ['remote', 'schedule'], 'persons': ['Bob']},\n        {'lossless_restatement': 'Team standup is at 9am daily', 'keywords': ['standup', 'team'], 'topic': 'meetings'}\n    ]\n}\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=60)\n\nresult = response.json()\nprint(f\"Added: {result.get('memories_added')}, Errors: {len(result.get('errors') or [])}\")",
            "javascript": "async function bulkAddMemory(baseUrl = '__BASE__', apiKey = null, agentId = null, memories = []) {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = `Bearer ${apiKey}`;\n  const res = await fetch(`${baseUrl}/bulk_add_memory`, { method: 'POST', headers, body: JSON.stringify({ agent_id: agentId, memories }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\nconst memories = [{ lossless_restatement: 'Test memory 1', keywords: ['test'] }];\nbulkAddMemory(null, null, '<AGENT_ID>', memories).then(console.log).catch(console.error);"
          }
        }
      ]
    },
    {
      "name": "Data Portability",
      "endpoints": [
        {
          "path": "/export_memories",
          "method": "POST",
          "summary": "Export all memories",
          "description": "Export all memories for an agent as JSON for backup, migration, or analysis. Returns a complete backup that can be imported to another agent or restored later.",
          "request": {
            "agent_id": "string",
            "format": "json"
          },
          "response": {
            "ok": true,
            "export": {
              "version": "1.0",
              "export_timestamp": "string",
              "agent_id": "string",
              "agent_name": "string",
              "total_memories": 0,
              "memories": []
            }
          },
          "when_to_use": "Call this to create a full JSON backup of all memories for an agent. Use for data portability, migration between environments, or offline analysis.",
          "use_cases": [
            "Creating a backup before running destructive operations",
            "Migrating an agent's memory from staging to production",
            "Providing a data export for GDPR data portability requests",
            "Downloading memories for offline analysis or reporting"
          ],
          "notes": "The export includes all memory entries with their full metadata. Large exports may take time — use a generous timeout. The returned format is compatible with /import_memories for restoration.",
          "errors": [
            {"code": 400, "message": "Bad request — missing agent_id"},
            {"code": 401, "message": "Unauthorized — missing or invalid API key"},
            {"code": 404, "message": "Not found — agent or memory system does not exist"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/export_memories' -H 'Content-Type: application/json' -H 'Authorization: Bearer <API_KEY>' -d '{\"agent_id\":\"<AGENT_ID>\"}'",
            "python": "import requests\nimport json\n\nurl = \"__BASE__/export_memories\"\npayload = { 'agent_id': '<AGENT_ID>' }\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=120)\n\nresult = response.json()\nexport_data = result.get('export')\nprint(f\"Exported {export_data.get('total_memories')} memories\")\n\n# Save to file\nwith open('memories_backup.json', 'w') as f:\n    json.dump(export_data, f, indent=2)\nprint('Saved to memories_backup.json')",
            "javascript": "async function exportMemories(baseUrl = '__BASE__', apiKey = null, agentId = null) {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = `Bearer ${apiKey}`;\n  const res = await fetch(`${baseUrl}/export_memories`, { method: 'POST', headers, body: JSON.stringify({ agent_id: agentId }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\nexportMemories(null, null, '<AGENT_ID>').then(r => {\n  console.log('Exported', r.export.total_memories, 'memories');\n  // Save: require('fs').writeFileSync('backup.json', JSON.stringify(r.export, null, 2));\n}).catch(console.error);"
          }
        },
        {
          "path": "/import_memories",
          "method": "POST",
          "summary": "Import memories from backup",
          "description": "Import memories from a previously exported JSON backup. Supports 'append' mode to add to existing memories or 'replace' mode to clear existing memories first.",
          "request": {
            "agent_id": "string",
            "export_data": {
              "version": "1.0",
              "memories": []
            },
            "merge_mode": "append"
          },
          "response": {
            "ok": true,
            "agent_id": "string",
            "merge_mode": "append",
            "memories_imported": 0,
            "source_agent": "string",
            "source_timestamp": "string",
            "entry_ids": [],
            "errors": null
          },
          "when_to_use": "Call this to restore memories from a previous export or migrate memories from one agent to another. Supports append (additive) or replace (destructive) modes.",
          "use_cases": [
            "Restoring memories from a backup after accidental deletion",
            "Cloning an agent's memory to a new agent in a different environment",
            "Merging memories from a decommissioned agent into an active one",
            "Seeding a new agent with memories exported from a template agent"
          ],
          "notes": "Use merge_mode \"append\" to add without affecting existing memories, or \"replace\" to clear and reimport. The export_data format must match what /export_memories produces. Large imports may take significant time.",
          "errors": [
            {"code": 400, "message": "Bad request — missing agent_id or invalid export_data format"},
            {"code": 401, "message": "Unauthorized — missing or invalid API key"},
            {"code": 404, "message": "Not found — target agent or memory system does not exist"},
            {"code": 422, "message": "Unprocessable entity — export_data version is incompatible"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/import_memories' -H 'Content-Type: application/json' -H 'Authorization: Bearer <API_KEY>' -d '{\"agent_id\":\"<TARGET_AGENT_ID>\",\"export_data\":{\"memories\":[{\"lossless_restatement\":\"Test\"}]},\"merge_mode\":\"append\"}'",
            "python": "import requests\nimport json\n\n# Load backup file\nwith open('memories_backup.json', 'r') as f:\n    export_data = json.load(f)\n\nurl = \"__BASE__/import_memories\"\npayload = {\n    'agent_id': '<TARGET_AGENT_ID>',\n    'export_data': export_data,\n    'merge_mode': 'append'  # or 'replace' to clear existing\n}\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=120)\n\nresult = response.json()\nprint(f\"Imported {result.get('memories_imported')} memories\")\nif result.get('errors'):\n    print(f\"Errors: {len(result.get('errors'))}\")",
            "javascript": "async function importMemories(baseUrl = '__BASE__', apiKey = null, agentId = null, exportData = null, mergeMode = 'append') {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = `Bearer ${apiKey}`;\n  const res = await fetch(`${baseUrl}/import_memories`, { method: 'POST', headers, body: JSON.stringify({ agent_id: agentId, export_data: exportData, merge_mode: mergeMode }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\n// const backup = require('./backup.json');\n// importMemories(null, null, '<AGENT_ID>', backup).then(console.log).catch(console.error);"
          }
        },
        {
          "path": "/memory_summary",
          "method": "POST",
          "summary": "AI memory summary",
          "description": "Generate an AI-powered summary of all agent memories. Optionally focus on a specific topic. Returns summary along with metadata about topics and persons mentioned.",
          "request": {
            "agent_id": "string",
            "focus_topic": "string (optional)",
            "summary_length": "medium"
          },
          "response": {
            "ok": true,
            "agent_id": "string",
            "summary": "string",
            "memory_count": 0,
            "topics_covered": [],
            "persons_mentioned": [],
            "focus_topic": null,
            "summary_length": "medium"
          },
          "when_to_use": "Call this to get an AI-generated natural language summary of everything an agent remembers, optionally focused on a specific topic.",
          "use_cases": [
            "Showing a user a digest of what the agent knows about them",
            "Generating a briefing document from an agent's accumulated knowledge",
            "Summarising a project's history from stored memories before a meeting",
            "Creating a topic-focused report from an agent's memory"
          ],
          "notes": "This triggers LLM inference and may take 10-60 seconds. summary_length accepts \"brief\", \"medium\", or \"detailed\". Use focus_topic to narrow the summary to a specific subject. Useful for human review of agent knowledge.",
          "errors": [
            {"code": 400, "message": "Bad request — missing agent_id"},
            {"code": 401, "message": "Unauthorized — missing or invalid API key"},
            {"code": 404, "message": "Not found — memory system not initialised or no memories exist"},
            {"code": 503, "message": "Service unavailable — LLM summarisation failed"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/memory_summary' -H 'Content-Type: application/json' -H 'Authorization: Bearer <API_KEY>' -d '{\"agent_id\":\"<AGENT_ID>\",\"summary_length\":\"medium\"}'",
            "python": "import requests\n\nurl = \"__BASE__/memory_summary\"\npayload = {\n    'agent_id': '<AGENT_ID>',\n    'focus_topic': None,  # Optional: focus on specific topic\n    'summary_length': 'medium'  # 'brief', 'medium', or 'detailed'\n}\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=120)\n\nresult = response.json()\nprint('Summary:')\nprint(result.get('summary'))\nprint(f\"\\nBased on {result.get('memory_count')} memories\")\nprint(f\"Topics: {result.get('topics_covered')}\")\nprint(f\"Persons: {result.get('persons_mentioned')}\")",
            "javascript": "async function memorySummary(baseUrl = '__BASE__', apiKey = null, agentId = null, options = {}) {\n  baseUrl = baseUrl === '__BASE__' ? (typeof process !== 'undefined' && process.env && process.env.BASE_URL) : baseUrl;\n  if (!baseUrl || baseUrl === '__BASE__') throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const headers = { 'Content-Type': 'application/json' };\n  if (apiKey) headers['Authorization'] = `Bearer ${apiKey}`;\n  const res = await fetch(`${baseUrl}/memory_summary`, { method: 'POST', headers, body: JSON.stringify({ agent_id: agentId, ...options }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\nmemorySummary(null, null, '<AGENT_ID>', { summary_length: 'detailed' }).then(r => {\n  console.log('Summary:', r.summary);\n  console.log('Based on', r.memory_count, 'memories');\n}).catch(console.error);"
          }
        }
      ]
    },
    {
      "name": "API Test Examples",
      "endpoints": [
        {
          "path": "/memory",
          "method": "GET",
          "summary": "Memory page (UI)",
          "description": "Returns the memory page (may redirect for unauthenticated users).",
          "request": null,
          "response": {
            "html": "string"
          },
          "when_to_use": "Use this endpoint to load the memory management UI page in a browser. It renders the interactive memory interface for the authenticated user.",
          "use_cases": [
            "Navigating to the memory page in a web application",
            "Testing that the memory UI renders correctly after deployment",
            "Embedding the memory page in an iframe within a larger admin tool"
          ],
          "notes": "Returns HTML, not JSON. Unauthenticated requests may be redirected to a login page. This is a UI endpoint, not an API endpoint — use /read_memory or /list_memories for programmatic access.",
          "errors": [
            {"code": 302, "message": "Redirect — user is not authenticated, redirecting to login"},
            {"code": 401, "message": "Unauthorized — session expired or invalid"}
          ],
          "code": {
            "curl": "curl -X GET '__BASE__/memory' -v",
            "python": "import requests\n\nurl = \"__BASE__/memory\"\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.get(url, headers=headers, timeout=15)\n\nprint(response.text)",
            "javascript": "// Node 18+ or browser\nasync function getMemory(baseUrl = null) {\n  baseUrl = baseUrl || (typeof process !== 'undefined' && process.env && process.env.BASE_URL);\n  if (!baseUrl) throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const res = await fetch(`${baseUrl}/memory`);\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.text();\n}\n\ngetMemory().then(t => console.log(t)).catch(e => console.error(e));"
          }
        },
        {
          "path": "/web/get_sessions",
          "method": "GET",
          "summary": "Get sessions (API)",
          "description": "GET sessions by id query parameter. Returns JSON list or object.",
          "request": {
            "id": "uuid (query)"
          },
          "response": {
            "sessions": []
          },
          "when_to_use": "Call this to retrieve chat session metadata by session ID. Use it to look up session details or verify a session exists before sending messages.",
          "use_cases": [
            "Loading a previous chat session to display its history",
            "Verifying that a session ID is valid before calling /web/chat",
            "Listing all sessions for a user in a session picker UI"
          ],
          "notes": "Pass the session id as a query parameter. Returns session metadata including creation time and associated user. If no id is provided, may return all sessions for the authenticated user.",
          "errors": [
            {"code": 400, "message": "Bad request — missing or malformed session id parameter"},
            {"code": 401, "message": "Unauthorized — missing or invalid credentials"},
            {"code": 404, "message": "Not found — session id does not exist"}
          ],
          "code": {
            "curl": "curl -G '__BASE__/web/get_sessions' --data-urlencode 'id=51ebe8c7-201b-4275-9bc5-44d7222f3509'",
            "python": "import requests\n\nurl = \"__BASE__/web/get_sessions\"\nparams = { 'id': '51ebe8c7-201b-4275-9bc5-44d7222f3509' }\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.get(url, params=params, headers=headers, timeout=15)\n\nprint(response.text)",
            "javascript": "async function getSessions(baseUrl = null, sessionId = '51ebe8c7-201b-4275-9bc5-44d7222f3509') {\n  baseUrl = baseUrl || (typeof process !== 'undefined' && process.env && process.env.BASE_URL);\n  if (!baseUrl) throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const url = new URL(`${baseUrl}/web/get_sessions`);\n  url.searchParams.set('id', sessionId);\n  const res = await fetch(url.href);\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\ngetSessions().then(j => console.log(j)).catch(e => console.error(e));"
          }
        },
        {
          "path": "/web/rag",
          "method": "POST",
          "summary": "RAG search",
          "description": "Run a RAG-style search (POST JSON).",
          "request": {
            "user_id": "string",
            "query": "string",
            "top_k": 3
          },
          "response": {
            "results": []
          },
          "when_to_use": "Call this to perform a RAG-style semantic search against a user's stored data. Returns ranked document chunks without generating an LLM answer.",
          "use_cases": [
            "Powering a search bar in the web UI with semantic results",
            "Fetching relevant context before constructing a custom prompt",
            "Debugging retrieval quality by inspecting raw search results",
            "Building a citation list showing which stored content matches a query"
          ],
          "notes": "Unlike /get_context, this does not generate an LLM answer — it only retrieves matching chunks. top_k controls how many results to return (default 3). Results are ranked by relevance score.",
          "errors": [
            {"code": 400, "message": "Bad request — missing user_id or query"},
            {"code": 401, "message": "Unauthorized — missing or invalid credentials"},
            {"code": 404, "message": "Not found — no data exists for this user_id"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/web/rag' -H 'Content-Type: application/json' -d '{\"user_id\":\"51ebe8c7-201b-4275-9bc5-44d7222f3509\",\"query\":\"test query\",\"top_k\":3}'",
            "python": "import requests\n\nurl = \"__BASE__/web/rag\"\npayload = { 'user_id': '51ebe8c7-201b-4275-9bc5-44d7222f3509', 'query': 'test query', 'top_k': 3 }\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=15)\n\nprint(response.text)",
            "javascript": "async function ragSearch(baseUrl = null, userId = '51ebe8c7-201b-4275-9bc5-44d7222f3509', query = 'test query', topK = 3) {\n  baseUrl = baseUrl || (typeof process !== 'undefined' && process.env && process.env.BASE_URL);\n  if (!baseUrl) throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const res = await fetch(`${baseUrl}/web/rag`, {\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({ user_id: userId, query, top_k: topK })\n  });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\nragSearch().then(r => console.log(r)).catch(e => console.error(e));"
          }
        },
        {
          "path": "/validate_key",
          "method": "POST",
          "summary": "Validate API key",
          "description": "Validate API key. POST JSON {api_key: string}. Returns {valid: bool}.",
          "request": {
            "api_key": "string"
          },
          "response": {
            "valid": true
          },
          "when_to_use": "Call this to check whether an API key is valid and active before using it in subsequent requests. Useful for key verification flows in onboarding or settings pages.",
          "use_cases": [
            "Validating a user-entered API key in a settings form before saving",
            "Checking key validity during application startup",
            "Implementing a key rotation workflow that verifies the new key works"
          ],
          "notes": "Returns {valid: true} if the key is active and has not been revoked. Does not reveal which permissions the key has — only that it is recognized. Use this as a lightweight pre-check rather than making a full API call.",
          "errors": [
            {"code": 400, "message": "Bad request — missing api_key field in request body"},
            {"code": 500, "message": "Internal server error — validation service unavailable"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/validate_key' -H 'Content-Type: application/json' -d '{\"api_key\":\"sk-...\"}'",
            "python": "import requests\n\nurl = \"__BASE__/validate_key\"\npayload = { 'api_key': 'sk-...'}\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=10)\n\nprint(response.text)",
            "javascript": "async function validateKey(baseUrl = null, apiKey = null) {\n  baseUrl = baseUrl || (typeof process !== 'undefined' && process.env && process.env.BASE_URL);\n  if (!baseUrl) throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const res = await fetch(`${baseUrl}/validate_key`, {\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify(apiKey ? { api_key: apiKey } : {})\n  });\n  return { status: res.status, body: await res.json() };\n}\nvalidateKey(null).then(console.log).catch(console.error);"
          }
        },
        {
          "path": "/web/create_session",
          "method": "POST",
          "summary": "Create chat session",
          "description": "Create a new chat session. Returns thread_id.",
          "request": {
            "user_id": "string"
          },
          "response": {
            "thread_id": "string"
          },
          "when_to_use": "Call this to initialize a new chat session for a user. The returned thread_id is required by /web/chat to send messages within that session.",
          "use_cases": [
            "Starting a new conversation thread when a user clicks \"New Chat\"",
            "Creating a session programmatically for automated testing",
            "Initializing a support conversation from an external trigger (e.g., email or webhook)"
          ],
          "notes": "Store the returned thread_id — it is required for all subsequent /web/chat calls in this conversation. Each session maintains its own message history independently.",
          "errors": [
            {"code": 400, "message": "Bad request — missing user_id"},
            {"code": 401, "message": "Unauthorized — missing or invalid credentials"},
            {"code": 500, "message": "Internal server error — session creation failed"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/web/create_session' -H 'Content-Type: application/json' -d '{\"user_id\":\"51ebe8c7-201b-4275-9bc5-44d7222f3509\"}'",
            "python": "import requests\n\nurl = \"__BASE__/web/create_session\"\npayload = { 'user_id': '51ebe8c7-201b-4275-9bc5-44d7222f3509' }\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=10)\n\nprint(response.text)",
            "javascript": "async function createSession(baseUrl = null, userId = '51ebe8c7-201b-4275-9bc5-44d7222f3509') {\n  baseUrl = baseUrl || (typeof process !== 'undefined' && process.env && process.env.BASE_URL);\n  if (!baseUrl) throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const res = await fetch(`${baseUrl}/web/create_session`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ user_id: userId }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.json();\n}\ncreateSession().then(console.log).catch(console.error);"
          }
        },
        {
          "path": "/web/chat",
          "method": "POST",
          "summary": "Send chat message (streaming)",
          "description": "Send a chat message to the streaming chat endpoint. Example shows a simple non-streaming POST consumer.",
          "request": {
            "thread_id": "string",
            "user_id": "string",
            "message": "string",
            "history": []
          },
          "response": {
            "reply": "string / stream"
          },
          "when_to_use": "Call this to send a user message and receive an AI response within an established chat session. Supports streaming responses for real-time display.",
          "use_cases": [
            "Powering the main chat interface in a web application",
            "Sending follow-up messages in an ongoing conversation thread",
            "Building a chatbot widget that streams responses token-by-token",
            "Automating multi-turn conversations for testing or scripting"
          ],
          "notes": "Requires a valid thread_id from /web/create_session. The history array is optional — the server maintains history server-side, but you can pass it for context if needed. Response may be streamed (text/event-stream) depending on client headers.",
          "errors": [
            {"code": 400, "message": "Bad request — missing thread_id, user_id, or message"},
            {"code": 401, "message": "Unauthorized — missing or invalid credentials"},
            {"code": 404, "message": "Not found — thread_id does not exist or has expired"},
            {"code": 503, "message": "Service unavailable — LLM provider is temporarily unreachable"}
          ],
          "code": {
            "curl": "curl -X POST '__BASE__/web/chat' -H 'Content-Type: application/json' -d '{\"thread_id\":\"local_thread_123\",\"user_id\":\"51ebe8c7-201b-4275-9bc5-44d7222f3509\",\"message\":\"Hello\",\"history\":[]}'",
            "python": "import requests\n\nurl = \"__BASE__/web/chat\"\npayload = { 'thread_id': 'local_thread_123', 'user_id': '51ebe8c7-201b-4275-9bc5-44d7222f3509', 'message': 'Hello', 'history': [] }\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.post(url, json=payload, headers=headers, timeout=120)\n\nprint(response.text)",
            "javascript": "async function sendChat(baseUrl = null, threadId = 'local_thread_123', userId = '51ebe8c7-201b-4275-9bc5-44d7222f3509', message = 'Hello') {\n  baseUrl = baseUrl || (typeof process !== 'undefined' && process.env && process.env.BASE_URL);\n  if (!baseUrl) throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const res = await fetch(`${baseUrl}/web/chat`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ thread_id: threadId, user_id: userId, message, history: [] }) });\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return await res.text();\n}\nsendChat().then(console.log).catch(console.error);"
          }
        },
        {
          "path": "/ping",
          "method": "GET",
          "summary": "Ping server",
          "description": "Simple health check for the running server.",
          "request": null,
          "response": {
            "status": "string"
          },
          "when_to_use": "Call this as a lightweight connectivity check to verify the server is running and reachable. Does not require authentication.",
          "use_cases": [
            "Health check probes from a load balancer or container orchestrator",
            "Client-side connectivity test before making authenticated API calls",
            "Automated monitoring to detect service outages"
          ],
          "notes": "This is the simplest endpoint — no auth required, minimal processing. Returns a status string. Use /health_detailed if you need to verify individual backend service health.",
          "errors": [
            {"code": 503, "message": "Service unavailable — server is starting up or shutting down"}
          ],
          "code": {
            "curl": "curl -X GET '__BASE__/ping' -v",
            "python": "import requests\n\nurl = \"__BASE__/ping\"\nheaders = {\n    \"Authorization\": \"Bearer <token>\",\n    \"Content-Type\": \"application/json\"\n}\n\nresponse = requests.get(url, headers=headers, timeout=10)\n\nprint(response.text)",
            "javascript": "async function ping(baseUrl = null) {\n  baseUrl = baseUrl || (typeof process !== 'undefined' && process.env && process.env.BASE_URL);\n  if (!baseUrl) throw new Error('baseUrl must be provided or set BASE_URL env var');\n  const res = await fetch(`${baseUrl}/ping`);\n  return { status: res.status, text: await res.text() };\n}\nping().then(console.log).catch(console.error);"
          }
        }
      ]
    },
    {
      "name": "MCP Server (Claude Integration)",
      "endpoints": [
        {
          "path": "mcp://create_memory",
          "method": "MCP_TOOL",
          "summary": "Initialize memory system (MCP)",
          "description": "MCP Tool: Create/initialize a SimpleMem memory system for an agent. This creates a ChromaDB collection for storing memory entries.",
          "request": {
            "agent_id": "string",
            "clear_db": false
          },
          "response": {
            "ok": true,
            "message": "string",
            "agent_id": "string"
          },
          "when_to_use": "Use this MCP tool in Claude Desktop to initialize the memory system for an agent before storing any memories via MCP.",
          "use_cases": [
            "Setting up a new agent's memory system through Claude Desktop",
            "Resetting an agent's memory during a Claude-driven development session",
            "Preparing an agent for memory ingestion as part of an automated MCP workflow"
          ],
          "notes": "Must be called before any other memory MCP tools for a given agent. Set clear_db to true only if you want to erase all existing memories. This is the MCP equivalent of the /create_memory HTTP endpoint.",
          "errors": [
            {"code": 0, "message": "Tool returned error — agent_id is missing or invalid"},
            {"code": 0, "message": "Tool returned error — vector database initialization failed"}
          ],
          "code": {
            "curl": "N/A - This is an MCP tool for Claude Desktop integration",
            "python": "# MCP Tool - Called by Claude directly\n# To use, configure Claude Desktop with:\n# claude_desktop_config.json\n\n# Example invocation from Claude:\n# \"Create a memory system for agent 'my-agent'\"",
            "javascript": "// MCP Tool - Called by Claude directly\n// Configure in Claude Desktop settings\n// See MCP_README.md for setup instructions"
          }
        },
        {
          "path": "mcp://process_raw_dialogues",
          "method": "MCP_TOOL",
          "summary": "Process dialogues via LLM (MCP)",
          "description": "MCP Tool: Process raw dialogues through LLM to extract structured memory entries. Flow: ADD_DIALOGUE → LLM → JSON RESPONSE → N Memory units → Vector Store.",
          "request": {
            "agent_id": "string",
            "dialogues": [
              {
                "speaker": "string",
                "content": "string",
                "timestamp": "string (optional)"
              }
            ]
          },
          "response": {
            "ok": true,
            "dialogues_processed": 0
          },
          "when_to_use": "Use this MCP tool to have Claude process raw conversation text into structured memory entries via LLM extraction.",
          "use_cases": [
            "Asking Claude to memorize a conversation transcript you paste into the chat",
            "Processing meeting notes into structured memory through Claude Desktop",
            "Extracting facts from unstructured text for long-term agent memory"
          ],
          "notes": "This triggers LLM processing and may take longer than direct memory tools. Each dialogue object needs at minimum a speaker and content field. Timestamp is optional but improves temporal recall.",
          "errors": [
            {"code": 0, "message": "Tool returned error — agent_id is missing or memory system not initialized"},
            {"code": 0, "message": "Tool returned error — dialogues array is empty or malformed"},
            {"code": 0, "message": "Tool returned error — LLM processing failed"}
          ],
          "code": {
            "curl": "N/A - MCP Tool",
            "python": "# MCP Tool - Example Claude prompt:\n# \"Process these dialogues for agent 'my-agent':\n# - Alice: Meeting at 2pm tomorrow\n# - Bob: I'll bring the documents\"",
            "javascript": "// MCP Tool - Invoked via Claude Desktop"
          }
        },
        {
          "path": "mcp://add_memory_direct",
          "method": "MCP_TOOL",
          "summary": "Direct memory save (MCP)",
          "description": "MCP Tool: Directly save pre-structured memory entries without LLM processing. Use when you have pre-structured memory data.",
          "request": {
            "agent_id": "string",
            "memories": [
              {
                "lossless_restatement": "string",
                "keywords": [],
                "persons": []
              }
            ]
          },
          "response": {
            "ok": true,
            "entries_added": 0,
            "entry_ids": []
          },
          "when_to_use": "Use this MCP tool to store a pre-structured memory entry directly without LLM processing. Ideal when Claude already has the structured data ready.",
          "use_cases": [
            "Saving a specific fact the user explicitly states (e.g., \"Remember that I prefer dark mode\")",
            "Storing structured data from a tool output directly into agent memory",
            "Adding memories in bulk through a Claude-driven automation script"
          ],
          "notes": "Bypasses LLM extraction — the memory is stored exactly as provided. Ensure lossless_restatement is a complete, self-contained statement. Include relevant keywords to improve future retrieval.",
          "errors": [
            {"code": 0, "message": "Tool returned error — agent_id is missing or memory system not initialized"},
            {"code": 0, "message": "Tool returned error — memories array is empty or missing lossless_restatement"}
          ],
          "code": {
            "curl": "N/A - MCP Tool",
            "python": "# MCP Tool - Example Claude prompt:\n# \"Add this memory for agent 'my-agent':\n# Alice scheduled a meeting with Bob at Starbucks\"",
            "javascript": "// MCP Tool - Invoked via Claude Desktop"
          }
        },
        {
          "path": "mcp://search_memory",
          "method": "MCP_TOOL",
          "summary": "Hybrid search (MCP)",
          "description": "MCP Tool: Search memories using hybrid retrieval combining semantic, keyword, and structured search.",
          "request": {
            "agent_id": "string",
            "query": "string",
            "top_k": 5,
            "enable_reflection": false
          },
          "response": {
            "ok": true,
            "results": []
          },
          "when_to_use": "Use this MCP tool to search an agent's memories by semantic similarity, keywords, or metadata filters from within Claude Desktop.",
          "use_cases": [
            "Asking Claude to recall what it knows about a specific topic",
            "Looking up memories related to a person or event during a conversation",
            "Verifying whether a fact has already been stored before adding a duplicate"
          ],
          "notes": "Uses hybrid retrieval combining semantic search, keyword matching, and metadata filtering. Set enable_reflection to true for deeper multi-pass retrieval at the cost of latency. top_k defaults to 5 results.",
          "errors": [
            {"code": 0, "message": "Tool returned error — agent_id is missing or memory system not initialized"},
            {"code": 0, "message": "Tool returned error — query is empty"}
          ],
          "code": {
            "curl": "N/A - MCP Tool",
            "python": "# MCP Tool - Example Claude prompt:\n# \"Search memories for 'my-agent' about meetings\"",
            "javascript": "// MCP Tool - Invoked via Claude Desktop"
          }
        },
        {
          "path": "mcp://get_context_answer",
          "method": "MCP_TOOL",
          "summary": "Q&A with memory (MCP)",
          "description": "MCP Tool: Get a context-aware answer using memory. Full Q&A flow: Query → HybridRetrieval → AnswerGenerator → Response.",
          "request": {
            "agent_id": "string",
            "question": "string"
          },
          "response": {
            "ok": true,
            "answer": "string",
            "contexts_used": []
          },
          "when_to_use": "Use this MCP tool to ask a question and get an LLM-generated answer grounded in the agent's stored memories, including source citations.",
          "use_cases": [
            "Asking Claude to answer a question using only what it has memorized for an agent",
            "Getting a context-aware response that cites specific memory entries",
            "Querying an agent's knowledge base in a conversational way through MCP"
          ],
          "notes": "This runs full RAG: retrieval + LLM answer generation. The response includes contexts_used showing which memories were consulted. Use mcp://search_memory if you only want raw results without answer generation.",
          "errors": [
            {"code": 0, "message": "Tool returned error — agent_id is missing or memory system not initialized"},
            {"code": 0, "message": "Tool returned error — question is empty"},
            {"code": 0, "message": "Tool returned error — LLM answer generation failed"}
          ],
          "code": {
            "curl": "N/A - MCP Tool",
            "python": "# MCP Tool - Example Claude prompt:\n# \"For agent 'my-agent', answer: What did Alice discuss?\"",
            "javascript": "// MCP Tool - Invoked via Claude Desktop"
          }
        },
        {
          "path": "mcp://update_memory_entry",
          "method": "MCP_TOOL",
          "summary": "Update memory (MCP)",
          "description": "MCP Tool: Update an existing memory entry in ChromaDB.",
          "request": {
            "agent_id": "string",
            "entry_id": "string",
            "updates": {}
          },
          "response": {
            "ok": true,
            "message": "memory_updated"
          },
          "when_to_use": "Use this MCP tool to modify an existing memory entry — correct facts, add metadata, or update timestamps through Claude Desktop.",
          "use_cases": [
            "Correcting a stored fact when the user provides updated information",
            "Adding a person's name to a memory after identifying who was involved",
            "Updating the topic or keywords of a memory to improve retrieval"
          ],
          "notes": "Only include fields you want to change in the updates object. Updating lossless_restatement will re-compute the vector embedding. You need the entry_id which can be obtained from mcp://search_memory or mcp://list_all_memories.",
          "errors": [
            {"code": 0, "message": "Tool returned error — agent_id or entry_id is missing"},
            {"code": 0, "message": "Tool returned error — entry_id does not exist in this agent's memory"},
            {"code": 0, "message": "Tool returned error — updates object is empty"}
          ],
          "code": {
            "curl": "N/A - MCP Tool",
            "python": "# MCP Tool - Example Claude prompt:\n# \"Update memory entry XYZ for agent 'my-agent' with new topic\"",
            "javascript": "// MCP Tool - Invoked via Claude Desktop"
          }
        },
        {
          "path": "mcp://delete_memory_entries",
          "method": "MCP_TOOL",
          "summary": "Delete memories (MCP)",
          "description": "MCP Tool: Delete memory entries from ChromaDB by entry IDs.",
          "request": {
            "agent_id": "string",
            "entry_ids": [
              "string"
            ]
          },
          "response": {
            "ok": true,
            "deleted_count": 0
          },
          "when_to_use": "Use this MCP tool to permanently remove specific memory entries by ID when a user asks Claude to forget something.",
          "use_cases": [
            "User asks Claude to forget a specific piece of information",
            "Cleaning up incorrect or outdated memories identified during a conversation",
            "Removing sensitive data that was accidentally stored"
          ],
          "notes": "This is irreversible — deleted memories cannot be recovered. Get entry_ids from mcp://search_memory or mcp://list_all_memories first. Confirm with the user before deleting.",
          "errors": [
            {"code": 0, "message": "Tool returned error — agent_id is missing or memory system not initialized"},
            {"code": 0, "message": "Tool returned error — entry_ids array is empty"},
            {"code": 0, "message": "Tool returned error — one or more entry_ids do not exist"}
          ],
          "code": {
            "curl": "N/A - MCP Tool",
            "python": "# MCP Tool - Example Claude prompt:\n# \"Delete memory entry XYZ from agent 'my-agent'\"",
            "javascript": "// MCP Tool - Invoked via Claude Desktop"
          }
        },
        {
          "path": "mcp://list_all_memories",
          "method": "MCP_TOOL",
          "summary": "List all memories (MCP)",
          "description": "MCP Tool: List all memory entries for an agent.",
          "request": {
            "agent_id": "string",
            "limit": 50
          },
          "response": {
            "ok": true,
            "memories": []
          },
          "when_to_use": "Use this MCP tool to browse all memory entries stored for an agent, useful for auditing or finding specific entry IDs.",
          "use_cases": [
            "Showing the user everything the agent remembers about them",
            "Finding entry_ids for memories that need to be updated or deleted",
            "Auditing memory contents for quality control or debugging"
          ],
          "notes": "Returns up to the limit number of entries (default 50). For agents with many memories, you may not see all entries in one call. Results include entry_ids which are needed for update and delete operations.",
          "errors": [
            {"code": 0, "message": "Tool returned error — agent_id is missing or memory system not initialized"},
            {"code": 0, "message": "Tool returned error — invalid limit value"}
          ],
          "code": {
            "curl": "N/A - MCP Tool",
            "python": "# MCP Tool - Example Claude prompt:\n# \"List all memories for agent 'my-agent'\"",
            "javascript": "// MCP Tool - Invoked via Claude Desktop"
          }
        }
      ]
    }
  ]
}